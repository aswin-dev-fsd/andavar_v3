'use client'; import {useEffect} from 'react'; export default function Reveal(){useEffect(()=>{document.documentElement.classList.add('js-ready');const els=[...document.querySelectorAll<HTMLElement>('.focus-reveal')];const on=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const show=(e:HTMLElement)=>e.classList.add('is-visible');if(on){els.forEach(show);return}const ob=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&show(e.target as HTMLElement)),{rootMargin:'0px 0px -12% 0px',threshold:.08});els.forEach(e=>ob.observe(e));const t=setTimeout(()=>els.forEach(show),2600);return()=>{ob.disconnect();clearTimeout(t)}},[]);return null}
