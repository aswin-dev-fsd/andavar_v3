import Link from 'next/link';import Aperture from '@/components/ui/Aperture';export default function PageHero({locale,breadcrumb,title,lede,band='ground',size=470}:{locale:string;breadcrumb:string;title:string;lede:string;band?:'ground'|'warm'|'cool';size?:number}){return <section className={`section band-${band}`}><Aperture size={size} opacity={.5} className="-right-44 -top-56"/><div className="shell relative z-10"><nav aria-label="Breadcrumb" className="text-[15px] text-[var(--ink-3)]"><Link href={`/${locale}`}>Home</Link><span aria-hidden="true"> / </span><span>{breadcrumb}</span></nav><div className="mt-8 max-w-[800px]"><h1 className="display-1 max-w-[17ch]">{title}</h1><p className="lede mt-7 max-w-[46ch]">{lede}</p></div></div></section>}
