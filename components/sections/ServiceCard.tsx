import Link from 'next/link';import Aperture from '@/components/ui/Aperture';export default function ServiceCard({symptom,title,body,href}:{symptom:string;title:string;body?:string;href:string}){return <Link href={href} className="card card-hover relative flex min-h-[250px] flex-col overflow-hidden p-[clamp(26px,3.4vw,38px)] no-underline"><Aperture size={120} opacity={.75} className="-right-16 -top-16"/><div className="relative z-10 flex h-full flex-col"><div className="text-[15px] font-semibold text-[var(--amber)]">{symptom}</div><h3 className="h3 mt-3">{title}</h3>{body&&<p className="mt-4 text-[17px] leading-[1.6] text-[var(--ink-2)]">{body}</p>}<span className="mt-auto pt-7 font-semibold text-[var(--blue)]">Read more →</span></div></Link>}
